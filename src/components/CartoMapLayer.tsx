// import * as ReactLeaflet from 'react-leaflet';

// interface CartoMapLayerProps extends ReactLeaflet.MapLayerProps {
//   url: string;
// }

// class CartoMapLayer extends ReactLeaflet.MapLayer<CartoMapLayerProps> {
//   // async createLeafletElement(props: CartoMapLayerProps) {
//   //   const map = this.context.map;

//   //   const promise = new Promise((resolve, reject) => {
//   //     cartodb.createLayer(map, props.url)
//   //       .addTo(map)
//   //       .on('done', (layer: any) => {
//   //         console.log('done!');
//   //         console.dir(layer);

//   //         resolve(layer);
//   //       })
//   //       .on('error', (error: any) => {
//   //         console.error('error');
//   //         reject(error);
//   //       });
//   //   });

//   //   const layer = await promise;

//   //   return layer;
//   // }

//   createLeafletElement() {

//   }
// }

// export default CartoMapLayer;
